// Placeholder for types
export interface User {
  id: string;
  email: string;
  nombre: string;
  rol: string;
}

export interface Vehicle {
  id: string;
  placa: string;
  marca: string;
  modelo: string;
  estado: string;
}
